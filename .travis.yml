#.travis.yml

sudo: required

dist: trusty

language: node_js

node_js:
  - 7

before_script:
  - set -e
  - npm install
  - export DISPLAY=:99.0
  - sh -e /etc/init.d/xvfb start

install:
  - npm install

script:
  - npm run test
  - npm run lint